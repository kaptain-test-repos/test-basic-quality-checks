# Test Quality Only

E2E test repo for `basic-quality-checks` workflow in buildon-github-actions.

Tests all quality check options: conventional branches, conventional commits, slash blocking, double hyphen blocking, duplicate commit blocking.

Only workflow with no versioning — tests quality checks in isolation.
